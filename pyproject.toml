[project]
name = "workshop-ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "akshara gurjar", email = "aksharag406@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "scikit-learn>=1.9.1",
]

[project.scripts]
workshop-ml = "workshop_ml:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
